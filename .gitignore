# ---- 仅在本地保留，不上传到 GitHub ----

# 第三方课程仓库克隆（独立 git 仓库，且其中 .env 含真实 API key）
/agent learning/

# Python 虚拟环境 / 缓存
.venv/
venv/
__pycache__/
*.pyc

# 密钥类文件
.env
.env.local
.env.*.local
*.key
*.pem

# 系统与编辑器临时文件
Thumbs.db
desktop.ini
.DS_Store
